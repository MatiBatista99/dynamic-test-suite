package ar.cabal.jpos;

import org.jpos.space.Space;
import ar.cabal.jpos.space.SpaceFactory;
import org.jpos.iso.ISOMsg;
import org.jpos.util.LogEvent;

        ISOMsg m = (ISOMsg) message.clone();

        String campo63 = m.getString(63);
        String cuotas = campo63.substring(110,112);
        String plan = campo63.substring(116,117);
        if ("13".equals(cuotas) || "16".equals(cuotas)){
            m.set(63,campo63.substring(0,110) + "0" + cuotas.substring(1) + campo63.substring(112,116) + "6" + campo63.substring(117));
        }

        evt.addMessage("--- filtered message (pvs_ahora3y6_outgoing) --");
        evt.addMessage(m);
        message = m;
